{% extends 'base.html.twig' %}

{% block title %}Gérer les jeux vidéo{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}" />
    <style>
        .flash-message {
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 8px;
            font-weight: bold;
        }
        .flash-success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .flash-error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
{% endblock %}

{% block body %}
<main>
    <div class="admin-container media-list-page">
        <div class="dashboard-hero">
            <h1>🎮 Gérer les jeux vidéo</h1>
        </div>
        {% for message in app.flashes('success') %}
            <div class="flash-message flash-success">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="flash-message flash-error">
                {{ message }}
            </div>
        {% endfor %}

        <div class="actions-header">
            <a href="{{ path('app_jeu_new') }}" class="btn btn-action">
                Créer un nouveau jeu vidéo
            </a>
            
            <form method="get" action="{{ path('app_jeu_index') }}" class="search-container">
                <input type="text"
                       name="search"
                       value="{{ search }}"
                       placeholder="Rechercher un jeu vidéo..."
                       aria-label="Rechercher un jeu vidéo">
                <i class="fas fa-search"></i>
            </form>
        </div>

        <div class="card-grid">
            {% for jeu in jeux %}
                <div class="admin-card">
                    <div class="card-content">
                        <h3>{{ jeu.nom }}</h3>
                        <p><strong>ID:</strong> {{ jeu.id }}</p>
                    </div>

                    <div class="admin-card-actions">
                        <a href="{{ path('app_jeu_show', {'id': jeu.id}) }}" class="btn btn-info">Voir</a>
                        <a href="{{ path('app_jeu_edit', {'id': jeu.id}) }}" class="btn btn-warning">Modifier</a>
                        <form method="post"
                              action="{{ path('app_jeu_delete', {'id': jeu.id}) }}"
                              onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce jeu vidéo ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeu.id) }}">
                            <button type="submit" class="btn btn-danger">Supprimer</button>
                        </form>
                    </div>
                </div>
            {% else %}
                <p>
                    {% if search %}Aucun résultat ne correspond à "{{ search }}".{% else %}Aucun jeu vidéo trouvé.{% endif %}
                </p>
            {% endfor %}
        </div>

        {% if jeux.pageCount > 1 %}
            <div class="pagination-bar">
                {% if jeux.currentPageNumber > 1 %}
                    <a href="{{ path('app_jeu_index', {page: jeux.currentPageNumber - 1, search: search}) }}" class="btn btn-secondary">← Précédent</a>
                {% endif %}
                <span class="pagination-info">Page {{ jeux.currentPageNumber }} / {{ jeux.pageCount }}</span>
                {% if jeux.currentPageNumber < jeux.pageCount %}
                    <a href="{{ path('app_jeu_index', {page: jeux.currentPageNumber + 1, search: search}) }}" class="btn btn-secondary">Suivant →</a>
                {% endif %}
            </div>
        {% endif %}

        <div class="media-list-page-footer">
            <a href="{{ path('app_admin') }}" class="btn btn-secondary">← Retour au tableau de bord</a>
        </div>
    </div>
</main>
{% endblock %}