<div id="mythic-modal-overlay" class="mythic-modal-overlay" data-type="{{ type }}" data-csrf="{{ csrf_token('mythic-unlock') }}" style="display:none;">
    <div class="mythic-modal">
        <button type="button" class="mythic-modal-close" aria-label="Fermer">✖</button>
        <h2 class="mythic-modal-title"></h2>
        <ul class="mythic-requirements-list"></ul>
        <p class="mythic-modal-status"></p>
        <button type="button" class="mythic-unlock-btn" disabled>Débloquer</button>
    </div>
</div>
