{% extends 'base.html.twig' %}

{% block title %}{{ film.nom }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}" />
{% endblock %}

{% block body %}
<main>
    <div class="admin-container">
        <div class="dashboard-hero">
            <h1>🎞️ {{ film.nom }}</h1>
        </div>

        <div class="card-grid">
            {% for card in film.cards %}
                <div class="admin-card">
                    {% if card.imagePath %}
                        <img src="{{ asset(card.imagePath) }}" alt="{{ card.nom }}" class="card-image">
                    {% endif %}
                    <div class="card-content">
                        <h3>{{ card.nom }}</h3>
                        <p class="rarity-label">{{ card.rarity ? card.rarity.libelle : 'N/A' }}</p>
                    </div>
                    <div class="admin-card-actions">
                        <a href="{{ path('app_card_film_edit', {'id': card.id}) }}" class="btn btn-warning">Modifier</a>
                    </div>
                </div>
            {% else %}
                <p>Aucune carte associée à ce film.</p>
            {% endfor %}
        </div>

        <div class="media-list-page-footer">
            <a href="{{ path('app_film_index') }}" class="btn btn-secondary">← Retour à la liste des films</a>
        </div>
    </div>
</main>
{% endblock %}
